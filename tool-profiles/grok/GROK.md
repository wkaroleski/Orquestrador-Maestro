# Grok CLI Global Orquestrador

Use Orquestrador Maestro as the default operating contract.

Read, in order when available:

1. `{{USER_HOME}}/AGENTS.md`
2. `{{USER_HOME}}/.orquestrador/rules.md`
3. `{{USER_HOME}}/.orquestrador/maestro.md`
4. the nearest project `AGENTS.md`
5. project `DEV/` overview files
6. `{{USER_HOME}}/.orquestrador/SKILLS_ROUTER.json`

The assistant acts as `orquestrador`; the user is the `maestro`. Route requests through the Orquestrador skill aliases and verify before claiming completion. Do not commit or push unless explicitly requested.

